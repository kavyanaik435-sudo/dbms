1st one
SHOW DATABASE;
USE company;
CREATE TABLE EMPLOYEE(EMPNO INT(70),ENAME CHAR(12),JOB CHAR(12),MANAGER_NO INT(70),SAL DECIMAL(10,2),COMMISSION DECIMAL(10,2));
SHOW TABLES;
DESC EMPLOYEE;
CREATE USER IF NOT EXISTS 'SONY' IDENTIFIED BY 'MAY_04';
GRANT ALL PRIVILEDGES ON COMPANY.EMPLOYEE TO 'SONY';
^DBye
SHOW DATABASES;
USE COMPANY;
SHOW TABLES;
DESC EMPLOYEE;
SELECT * FROM EMPLOYEE;
INSERT INTO EMPLOYEE VALUES (101,'RAVANA','MANAGER',1234,1000);
COMMIT;
SELECT * FROM EMPLOYEE;
INSERT INTO EMPLOYEE VALUES(102,'RAM','DEVELOPER',4567,60000,20000);
SELECT * FROM EMPLOYEE;
INSERT INTO EMPLOYEE VALUES(103,'SITA','ADMIN',7890,70000,3000);
SELECT * FROM EMPLOYEE;
DELETE FROM EMPLOYEE WHERE ENAME='KAVANA';
SELECT * FROM EMPLOYEE;
ROLLBACK;
SELECT * FROM EMPLOYEE;
DESC EMPLOYEE;
ALTER TABLE EMPLOYEE MODIFY ENAME CHAR(12) NOT NULL,
MODIFY JOB CHAR(12) NOT NULL;

2ND ONE
SHOW DATABES;
USE COMPANY_02;
CREATE TABLE EMPLOYEE(EMPNO INT,ENAME VARCHAR(255),JOB VARCHAR(255),MGR INT,SAL DECIMAL(10,2));
SHOW TABLES;
DESC EMPLOYEE;
ALTER TABLE EMPLOYEE ADD COLUMN COMMISSION DECIMAL(10,2);
INSERT INTO EMPLOYEE(EMPNO,ENAME,JOB,MGR,SAL,COMMISSION)
VALUES
(101,'DHARMA','MANAGER',NULL,50000,1000),
SELECT * FROM EMPLOYEE;
UPDATE EMPLOYEE SET JOB=;SENIOR DEVELOPER' WHERE EMPNO=102;
SELECT * FROM EMPLOYEE;
ALTER TABLE EMPLOYEE CHANGE COLUMN MGR MANAGERR_ID INT;
DESC EMPLOYEE;
DELETE FROM EMPLOYEE WHERE EMPNO=105;
SELECT*FROM EMPLOYEE;

3RD ONE
CREATE DATABASE COMPANY_03;
USE COMPANY_03;
CREATE TABLE EMPLOYEE(E_ID INT PRIMARY KEY,ENAME VARCHAR(255),AGE INT,SALARY DECIMAL(10,2));
DESC EMPLOYEE;
INSERT INTO EMPLOYEE(E_ID,ENAME,AGE,SALARY)
VALUES(1,'RAM',30,50000),
SELECT * FROM EMPLOYEE;
SELECT COUNT(ENAME)AS TOTAL_EMPLOYEE FROM EMPLOYEE;
SELECT MAX(AGE)AS MAX_AGE FROM EMPLOYEE;
SELECT MIN(AGE)AS MIN_AGE FROM EMPLOYEE;
SELECT ENAME,SALARY FROM EMPLOYEE ORDER BY SALARY ASC;
SELECT SALARY COUNT(*)AS EMPLOYEE_COUNT FROM EMPLOYEE GROUP BY SALARY;
SELECT SUM (SALARY)AS TOTALL_SALARY FROM EMPLOYEE;

4TH ONE
CREATE DATAV=BASE LAB4;
USE LAB04;
CREATE TABLE CUSTOMERS(ID INT PRIMARY KEY AUTO_INCREMENT,NAME VARCHAR(255),AGE INT,ADDRESS VARCHAR(255),SALARY DECIMAL(10,2));
SHOW TABLES;
DESC CUSTOMERS;
INSERT TRIGGER
DELIMITER
CREATE TRIGGER after_insert_salary_difference
AFTER INSERT ON CUSTOMER
FOR EACH ROW
BEGIN
SET @my_sql_diff_CONCAT('salary inserted is:'NEW.SALARY);
END;//
UPDATE TRIGGER
DELIMITER
CREATE TRIGGER after_update_salary_difference
AFTER UPDATE ON CUSTOMER
FOR EACH ROW
BEGIN
DECLARE old_salary DECIMAL(10,2)
DECLARE new_salary DECIMAL(10,2)
SET old_salary=OLD.SALARY;
SET new_salary=NEW.SALARY;
SET@ my_sql_diff=CONCAT('salary difference after update is:",NEW.salary_old_salary);
END;//
DLETE TRIGGER
DELIMITER//
CREATE TRIGGER after_update_salary_difference
AFTER UPDATE ON CUSTOMERS
FOR EACH ROW
BEGIN
DECLARE old_salary DECIMAL(10,2);
DECLARE new_salary DECIMAL(10,2);
SET old_salary=OLD.SALARY;
SET new_salary=NEW.SALARY;
SET@ my_sql_diff=CONCAT('salary difference after update is:'NEW.SALARY_OLD.SALARY);
END;//
USE LAB04;
--TEST INSERT TRIGGER
INSERT INTO CUSTOMER(NAME,AGE,ADDRESS,SALARY)
VALUES
('KAVYA',35,'123 MAIN ST',20000);
SELECT @ my_sql_diff AS SAL_DIFF;
--TEST UPDATE TRIGGER
UPDATE CUSTOMERS
SET SALARY=50000 WHERE ID=1;
SELECT @ my_sql_sal_diff AS SAL.DIFF;
--TEST DELETE TRIGGER
DELETE FROM CUSTOMERS WHERE ID=1;
SELECT @ my_sql_sal_diff AS SAL.DIFF;

5TH ONE
USE LAB05;
CREATE TABLE EMPLOYEE(E_ID INT,ENAME VARCHAR(255),AGE INT,SALARY DECIMAL(10,2);
DESC EMPLOYEE;
INSERT INTO EMPLOYEE(E_ID,ENAME,AGE,SALARY)

VALUES
(1,'SAM',30,50000),
DELIMITER//
CREATE PROCEDURE fetch_emplo_data()
BEGIN
DECLARE emp_id INT;
DECLARE emp_name VARCHAR(255);
DECLARE emp_age INT;
DECLARE emp_salary DECIMAL(10,2);
DECLARE emp_cursor WRSDR FOR SELECT E_ID,ENAME,AGE,SALARY FROM EMPLOYEE;
DECLARE CONTINUE HADLER FOR NOT FOUND SET@ finished=1;
OPEN emp_cursor;
SET @ finished=0;
Cursor_loop;LOOP
FETCH emp_cursor INTO emp_id,emp_name,emp_age,emp_salary;
IF@ finished=1 THEN
LEAVE cursor_loop;
END IF;
SELECT CONCAT('Employee ID:'emp_id,',name:',emp_name,'age:'emp_age,'salary:',emp_salary)AS EMPLOYEE_INFO;
END LOOP;
CLOSE emp_cursor;
END//
USE LAB05;
CALL fetch_employee_data();

7th ONE
USE Student;
db.createCollection("Student");
db.Student.insertOne({name:"jphn doe",age:30});
db.Student.insertMany([{name:"john doe",age:25,sem:4},{name:"alice",age:29,sem:5}]);
db.Studentinfo.find();
db.student.find().pretty();
db.Student.findOne({name:"alice"});
db.student.insertMany([{name:"john doe",age:25},
{name:"krupa",age:30},
{name:"kavya",age:29},
{name:"devika",age:26}]);
db.Student.updateOne({name:"john doe"},{$set:{age:31}});
db.Student.find();
db.Customers.updateMany({age:{$gt:25}},{$inc:{age:1}});
db.Student.find();
db.Studentinfo.find();
db.Studentinfo.updateMany({age:{$gt:30}},{$set:{branch:"cse"}});
db.Studentinfo.find();
db.Studentinfo.replaceOne({name:"kavya"},{name:"kavya",age:20,city:"dharwad"});
db.Studentinfo.deleteOne({name:"kavya"});
db.Student.deleteMany({age:{$lt:25}});
db.Studentinfoo.find();







